[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --prefer-offline"]
cacheDirectories = ["/root/.npm"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
